/* Test program #3 */
/* In this test program there are simple syntax erros such a missing semi colons and braces */

int gcd(int u, int v { /* no paranthesis on this line: 4 */
  if (v == 0) return u;
  else return gcd(v, u);
}

void main(void) {
  int x; int y;
  x = input() /* missing semi here: 11 */
  y = input() /* missing semi here: 12 */
  output(gcd(x, y));
}
